import type { Category } from "../types"
//se le agrega el type que se creo para la bd en types, y se importa arriba
export const categories : Category[] = [
    { id: 1, name: 'Comida'},
    { id: 2, name: 'Ejercicio'}
  ]